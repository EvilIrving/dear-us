# 变更记录

## 1.1.0 · 2026-08-25

### 核心体验

- 首页从功能卡片列表改为共同房间与实体架子，三个容器按双方共同留下的总量表现积累状态。
- 新内容只以微弱光点提示，不使用聊天未读红点或消息列表。
- 星星、胶囊改为持续按住打开；纸团使用更长的持续按住动作，取代系统确认弹窗。
- 写入、打开和回应全部改为全屏场景，不再使用核心流程 Sheet 表单。
- 制作场景增加小屏与键盘高度适配，内容可滚动且放入/录音手势使用高优先级识别。
- 打开后的回应改成重新制作同类物件，不再在内容下方显示回复输入框。

### 文字与照片

- 文字输入改为容器对应的纸面书写场景。
- 使用向上拖动物件进入容器口的手势完成保存，并提供阈值触觉反馈。
- 照片改为相纸式选择、预览和可选说明文字。
- 新建界面暂停视频写入；保留 1.0 视频记录的读取与播放兼容。

### 语音

- 增加按住开始、左滑取消、松手完成的完整手势状态机。
- 第一次进入语音模式时预取麦克风权限，录音期间锁定关闭与模式切换。
- 增加录音准备态、强状态红点、十分之一秒时长和实时音量波形。
- 松手后直接保存到当前容器，不再要求二次确认。
- 增加最短录音保护、取消阈值触觉反馈、权限错误和场景内提示。
- 保存失败时保留语音草稿，支持再次推入或丢弃重录。
- 增加 VoiceOver 的开始、完成和取消录音辅助动作。
- 增加音频会话中断与进入后台保护，未完成录音不会卡在假录音状态。
- 音频播放控件改为自定义波形拖动，不再使用系统 Slider。

### 管理与辅助界面

- “我留下的”改为“我的抽屉”，使用物件筛选和纸张式存根。
- 设置页移除系统 Form 和删除确认弹窗；退出/删除改为持续按住两秒。
- 全局错误提示改为场景内 `WhisperNoticeBanner`。
- 创建共同空间改为持续按住空瓶子的引导动作。

### 技术

- 新增 `RitualUI.swift` 与 `VoiceHoldRecorderView.swift`。
- `AudioRecorder` 增加准备状态、实时电平和 50 ms 更新计时器。
- CloudKit Schema、记录字段和同步状态机保持不变，兼容 1.0 数据。
- 版本提升至 1.1.0，构建号提升至 2。
- 静态校验增加核心手势存在性与传统核心 UI 残留检查。

## 1.0.0 · 2026-08-25

### 产品

- 将单机 A/B 演示模式替换为真实 iCloud 双人共同空间。
- 完成星星瓶、胶囊盒和纸团篓的交换额度机制。
- 为纸团增加主动接住确认，避免负面内容被动弹出。
- 增加“我留下的”与对方打开状态。
- 保持无聊天列表、无在线状态、无任务和无游戏化经济系统。

### 内容

- 支持文字、照片、视频和语音。
- 每条内容支持一个 `CKAsset` 附件，单个附件上限 48 MB。
- 本机媒体副本使用 iOS 文件保护。

### CloudKit

- 使用自定义记录区和区域级 `CKShare` 实现邀请与共同读写。
- 为私有数据库和共享数据库分别建立 `CKSyncEngine`。
- 支持离线写入、待上传恢复、记录冲突合并、账号变化和换机关系发现。
- 支持系统共享面板、冷启动邀请接受、CloudKit 静默推送和弱内容通知。
- 增加共同空间退出、停止共享与删除处理。

### 工程

- 最低系统版本调整为 iOS 17。
- 增加 App 图标、Privacy Manifest、共享 Scheme、配置文件和发布文档。
- 无第三方依赖，无自建服务器。
