# Dear Us Demo UI、交互与动效约束 V2

> 状态：当前有效
>
> 更新：2026-08-28
>
> 适用范围：首页、容器详情、制作、打开、抽屉、设置、引导与所有后续 demo 页面。

本文档取代旧版“先完成写实核心物件，再进入界面”的执行顺序。当前阶段优先把软件逻辑、信息层级、核心路径和直接操作做完整。星星瓶、胶囊盒和纸团篓允许继续使用简单几何占位，不得因为美术未定而阻塞功能开发。

最终美术仍可在后续替换，但不得破坏本文定义的布局、状态、操作和反馈约束。

---

## 1. 设计目标

界面需要满足六个核心原则：

1. **直接操控（Direct Manipulation）**：用户直接拖动、按住、选择当前对象，不通过多层菜单或解释页面完成核心动作。
2. **即时反馈（Immediate Feedback）**：按下、拖动、越过阈值、保存、取消和失败都立即有视觉或触觉反馈。
3. **易于恢复（Forgiveness）**：误触可以松手取消；保存失败保留草稿；媒体导入失败不破坏上一份有效内容。
4. **连续性（Continuity）**：同一对象和状态在转场前后保持连续，不突然消失、跳位或换成无关页面。
5. **少阻塞**：除系统权限与共享边界外，不使用确认弹窗阻断核心流程。
6. **少等待**：本机保存成功即完成当前操作，CloudKit 在后台同步；网络不得阻塞放入或打开。

成功标准不是“看起来有设计”，而是：

- 用户第一眼知道自己在哪。
- 用户知道当前有什么状态。
- 用户知道下一步能做什么。
- 用户操作后立刻知道发生了什么。
- 用户出错后知道内容是否仍然保留。

---

## 2. 当前阶段的视觉优先级

按以下顺序工作，不得跳级：

1. **布局对齐**：网格、边距、基线、尺寸和安全区正确。
2. **信息层级**：标题、状态、主操作和次操作顺序清楚。
3. **交互状态**：可用、禁用、按下、处理中、成功、失败可辨认。
4. **文案精简**：删除重复说明与产品宣言。
5. **动效**：只为状态变化服务。
6. **材质与最终美术**：以上全部稳定后再替换占位资产。

任何页面如果仍有错位、遮挡、重复说明或无法理解的图标，不得继续增加阴影、渐变、粒子、3D 或复杂动画。

---

## 3. 全局布局约束

### 3.1 页面网格

- 页面左右内容边距：`20pt`。
- 同组元素间距优先使用：`8 / 12 / 16pt`。
- 不同区块间距优先使用：`16 / 20 / 24pt`。
- 页面最大内容宽度：`640pt`。
- 同一行卡片必须等宽、等高、顶部和底部对齐。
- 不使用负间距修正布局。
- 不使用跨层 `offset` 对齐标题、物件或装饰。
- 不允许一个元素的视觉位置依赖另一个独立层的“碰巧重合”。

### 3.2 安全区

- 顶部操作不得贴近状态栏或 Dynamic Island。
- 底部关键操作不得被 Home Indicator 遮挡。
- 可滚动页面必须保留底部安全留白。
- 页面标题和返回按钮必须属于同一纵向节奏，不能一个贴顶、一个悬空。

### 3.3 对齐

- 标题、正文、数字和图标必须使用明确的左对齐、居中或右对齐，不混用模糊基线。
- 数量使用等宽数字。
- 同组状态值使用相同字号和相同标签位置。
- 图标、标题、状态、数量必须放在同一组件内部完成布局，不能拆到不同坐标系。

### 3.4 卡片

- 首页与设置卡片圆角建议：`20–24pt`。
- 紧凑操作按钮圆角建议：`12–16pt`。
- 卡片边框 `1pt`；强调态最多 `1.5pt`。
- 不使用双层粗描边制造“高级感”。
- 阴影必须轻微；功能占位阶段可以完全不用阴影。

---

## 4. 信息层级与 UX 文案

### 4.1 文案职责

界面文案只负责：

- 标识页面或对象。
- 描述当前状态。
- 告诉用户下一步动作。
- 说明错误后内容是否保留。
- 说明不可逆操作的后果。

界面文案不负责：

- 重复解释产品理念。
- 用诗意句子替代状态。
- 描述用户眼前已经看见的内容。
- 为装饰图标补充意义。
- 在每个页面重复“这不是聊天”等产品宣言。

### 4.2 字数约束

- 页面标题：优先 `2–6` 个字。
- 按钮：优先 `2–8` 个字。
- 状态标签：优先 `2–6` 个字。
- 一般辅助说明：最多一行。
- 不可逆后果：最多两行。
- 空状态：一个标题 + 一句路径说明。

### 4.3 同一信息只表达一次

禁止同时出现：

- 数字“1 待打开”。
- 说明“可以打开一件”。
- 按钮“按住打开”。
- 辅助文案“持续按住后内容才会打开”。

一个页面应使用以下结构：

1. 状态数字或短状态。
2. 一个主操作。
3. 必要时一个取消规则。

其余重复说明删除。

### 4.4 清晰语言

优先：

- `按住打开`
- `松开取消`
- `留下一件`
- `待打开 1`
- `草稿已保留`
- `正在同步`
- `无法保存，立即重试`

避免：

- “瓶子愿意把一颗交给你”。
- “房间悄悄发生了一点变化”。
- “先照顾自己的感受”。
- “它知道里面有新的光”。

诗意语言可以存在于品牌内容或少量空状态中，但不能代替功能状态和操作标签。

---

## 5. 图标约束

### 5.1 功能图标必须可理解

允许无文字图标的情况：

- 返回或关闭。
- 系统约定非常明确的播放、暂停、删除。
- 已有清晰无障碍标签。

其他图标必须配文字，或直接删除。

### 5.2 禁止的图标用法

- 顶部单独悬浮的眼睛图标表示“本机预览”。
- 页面右上角重复显示抽屉、容器或设置图标。
- 用小圆点、萤火虫或发光粒子代替明确状态。
- 将核心物件 SF Symbol 放大后当作最终美术。
- 一个图标既表示页面身份，又看起来像可点击按钮。

### 5.3 页面身份不需要重复

如果页面标题已经是“抽屉”，右上角不得再放抽屉图标。

如果页面标题已经是“设置”，右上角不得再放设置或同步装饰。

如果页面标题已经是“纸团篓”，顶部不得再放纸团图标。

---

## 6. 核心组件尺寸约束

### 6.1 标准横向按钮

- 高度：`48–56pt`。
- 水平内边距：`16–20pt`。
- 图标尺寸：`14–18pt`。
- 标签使用 `subheadline` 或 `body` semibold。
- 按下缩放不小于 `0.96`。
- 按下反馈时长：`80–120ms`。

### 6.2 长按操作

长按用于防误触和主动选择，不用于制造仪式感。

必须：

- 使用横向 `52pt` 左右的按钮。
- 按住时在按钮内部显示连续进度。
- 松手立即复位。
- 完成时触发一次成功触觉。
- 标签直接说明动作，例如“按住打开”“按住删除共同空间”。

禁止：

- 直径 `90pt` 以上的巨大圆盘。
- 外围多层光环。
- 手掌图标占据页面视觉中心。
- 圆盘下再重复两行“按住”“松开取消”。
- 在设置页复用容器详情的仪式控件。

### 6.3 次操作

“留下一件”等次操作必须是紧凑横向按钮或清晰列表行：

- 高度 `48–56pt`。
- 图标、标题和可选短说明水平排列。
- 不使用一个大圆图标 + 标题 + 副标题占据 `120pt` 以上高度。

### 6.4 状态组

- 状态值和标签成组显示。
- 两个并列状态等宽。
- 状态组高度建议 `56–72pt`。
- 不同时使用珠点、数字、句子和发光效果表达同一状态。

---

## 7. 页面级约束

### 7.1 首页

首页只回答三个问题：

1. 三个容器分别是什么。
2. 每个容器有多少内容或多少待打开内容。
3. 点击后进入哪里。

布局：

- 一个全宽主卡 + 两个等宽次卡，或三个等权卡片。
- 卡片之间统一 `12pt` 间距。
- 所有信息必须在卡片内部。
- 不使用架子横条、负 offset 或跨层标题。
- 不使用无意义循环呼吸。
- 无待处理内容时不显示强提醒。

### 7.2 容器详情

信息顺序：

1. 返回。
2. 页面标题。
3. 一句必要规则。
4. 紧凑容器占位或最终资产。
5. 状态组。
6. 横向“按住打开”。
7. 横向“留下一件”。

尺寸建议：

- 占位物件高度不超过屏幕可用高度的 `25%`。
- 主操作与次操作均为横向按钮。
- 首屏必须能同时看到状态和至少一个核心操作。

禁止：

- 占位物件占据半屏。
- 物件下重复显示总量、状态句和状态数字。
- 巨型圆形按住控件。
- “留下新的”分割线与“留下一件”标题重复。

### 7.3 制作页

只保留三个层级：

1. 内容编辑区。
2. 类型切换。
3. 放入操作。

规则：

- 草稿恢复后立即可编辑。
- 模式切换反馈在 `160ms` 内完成。
- 保存成功后短过渡并关闭。
- 保存失败保留草稿，并提供原位重试。
- 不使用成功动画遮挡页面超过 `300ms`。
- 照片说明使用“添加说明（可选）”。
- 语音状态只显示录音、取消、时长和错误。

### 7.4 打开内容页

- 内容立即出现，不先等待装饰动画。
- 顶部只保留返回/收好和必要分类。
- 正文与媒体是视觉重点。
- 回应入口为紧凑次操作。
- 下拉关闭有连续跟手反馈。

### 7.5 抽屉

- 页面标题、分区切换、筛选、内容列表。
- 不在右上角重复抽屉图标。
- 空状态只显示：当前为空 + 内容如何出现。
- 卡片状态使用短标签，例如“等着”“看过”。

### 7.6 设置

设置页必须是紧凑列表，不是仪式场景。

- 一行一个事实或操作。
- 每行高度建议 `52–64pt`。
- 区块间距 `16pt` 左右。
- 行内最多标题 + 一行状态。
- 不在卡片底部重复说明。
- 隐私事实合并到对应行，不写长段落。
- 危险操作独立分区，只保留一行后果和一个横向长按按钮。
- 不使用巨大手掌圆盘。

### 7.7 引导

引导只保留三个路径：

- 创建共同空间。
- 本机预览。
- 打开已有邀请。

不重复产品宣言，不使用循环呼吸，不隐藏邀请加入方式。

---

## 8. 空状态与恢复状态

### 8.1 空状态结构

每个空状态必须包含：

- 简短标题：当前为空。
- 一句说明：什么操作会让内容出现。
- 核心入口：如果当前页面适合直接行动。

示例：

```text
还没有留下内容
从任一容器留下一件后，会显示在这里。
```

### 8.2 失败状态

失败提示必须回答：

1. 什么失败了。
2. 用户内容是否仍在。
3. 下一步是什么。

示例：

```text
保存失败
草稿已保留，可以立即重试。
```

### 8.3 同步状态

- 本机写入成功后立即完成当前交互。
- CloudKit 后台同步。
- 网络失败显示“等待网络同步”，不阻塞页面。
- 不在首页放无标签同步图标。
- 同步详情放在设置或明确状态行中。

---

## 9. 动效约束

### 9.1 允许的动效

- 按下：`80–120ms` 轻微缩放或透明度变化。
- 模式切换：`120–180ms`。
- 拖动：1:1 跟手。
- 长按：连续进度填充。
- 阈值：一次触觉与明确状态变化。
- 打开：`120–220ms` 状态过渡。
- 保存：短进度反馈；成功后尽快退出。
- 失败：原对象回到可操作位置。

### 9.2 禁止的动效

- 无状态原因的循环呼吸。
- 背景粒子持续漂浮。
- 卡片和图标反复缩放。
- 为“生动”添加的弹跳、闪光和旋转。
- 所有控件统一使用 spring。
- 成功前播放完整成功结局。
- 用动画掩盖网络等待。

### 9.3 动效必须回答

每个动效实现前必须明确：

- 哪个状态变成哪个状态？
- 用户是否触发了它？
- 能否取消？
- 失败后回到哪里？
- 不播放动画时，状态是否仍然清楚？

无法回答时，不添加该动效。

---

## 10. 颜色、背景与占位

### 10.1 背景

- 使用低对比暖色背景。
- 背景不承载关键状态。
- 不使用粒子、漂浮圆点或强光斑制造氛围。
- 页面内容必须在没有背景装饰时仍然成立。

### 10.2 状态色

- 星星：暖黄。
- 胶囊：低饱和绿。
- 纸团：暖灰。
- 危险操作：克制红色。
- 禁用状态：降低对比，但文字仍可读。

### 10.3 当前占位资产

当前容器可以使用 SF Symbol、圆角矩形和简单几何作为功能占位，但必须：

- 尺寸克制。
- 对齐正确。
- 不冒充最终美术。
- 不成为页面最大视觉焦点。
- 不重复显示页面标题。

等最终资产通过审美验收后，再替换占位；替换不得改变交互热区、信息层级和页面网格。

---

## 11. 无障碍

- 纯图标按钮必须有准确的 accessibility label。
- 长按操作提供等价 accessibility action。
- 状态不能只靠颜色表达。
- 动态字体下不得截断关键操作。
- VoiceOver 顺序必须符合视觉顺序。
- 开启减少动态效果时，保留进度和状态，不保留装饰位移。

---

## 12. 截图验收清单

每次启动模拟器后，按页面逐项检查：

### 布局

- [ ] 左右边距一致。
- [ ] 同行卡片等宽等高。
- [ ] 标题、图标、数字基线一致。
- [ ] 没有负间距造成的重叠。
- [ ] 没有架子、分割线或装饰压住文字。
- [ ] 首屏可以看到核心状态和核心操作。

### 文案

- [ ] 没有需要用户猜含义的无标签图标。
- [ ] 同一状态没有重复表达。
- [ ] 每个按钮直接说明动作。
- [ ] 空状态说明内容如何出现。
- [ ] 失败状态说明草稿是否保留。
- [ ] 不可逆操作明确说明后果。

### 交互

- [ ] 按下立即反馈。
- [ ] 拖动连续跟手。
- [ ] 长按有可见进度，松手复位。
- [ ] 保存和打开不等待网络。
- [ ] 失败后原内容仍可操作。
- [ ] 没有不必要弹窗。

### 视觉强度

- [ ] 没有巨大圆形仪式控件。
- [ ] 没有循环呼吸或漂浮装饰。
- [ ] 占位物件没有占据半屏。
- [ ] 设置页是紧凑列表。
- [ ] 页面身份没有被图标重复表达。

任何一项不通过，先修复该项，再继续美术和动效。

---

## 13. 当前明确禁止复现的问题

以下问题已经在模拟器截图中出现，后续不得复现：

1. 棕色横条架子压住容器标题和状态文字。
2. 主物件、标题和架子分属不同坐标系，依靠 offset 拼接。
3. 左上角单独出现无标签眼睛图标。
4. 抽屉右上角重复出现抽屉图标。
5. 容器详情右上角重复出现物件图标。
6. 设置卡片下方堆叠低对比说明文字。
7. 设置页使用巨大手掌圆盘执行离开或删除。
8. 容器详情使用巨大圆盘长按，并在下方重复“持续按住、松开取消”。
9. 巨大占位物件、状态数字、状态句和按钮重复表达同一件事。
10. 以“仪式感”为理由占用大面积首屏空间。

当前 demo 的评价顺序固定为：

> 对齐正确 > 状态清楚 > 操作直接 > 可恢复 > 动效有目的 > 最终美术。
