# 耳语 · Dear Us 1.1.0

“耳语”（英文名 `Dear Us`）是一款原生 iOS 双人关系应用。它不把关系表达做成消息列表，也不依赖任务、金币、角色或连续签到；两个人共同养着一个星星瓶、一只胶囊盒和一个纸团篓。容器会随着双方留下的文字、照片和语音逐渐发生变化。

1.1.0 保留 1.0.0 的 CloudKit 双人空间、离线同步与交换打开机制，重新实现了核心体验层。用户不再面对“输入框 + 附件按钮 + 提交按钮”的工具界面，而是完成写、折、封、揉、按住、滑动和放入等连续动作；制作场景在小屏与键盘出现时可自然滚动，核心手势仍优先响应。

## 1.1.0 体验变化

### 共同房间

首页从三张功能卡片改成一个安静的共同房间。三个容器直接摆在架子上，内容数量通过瓶中星星、药盒胶囊和纸篓纸团的积累表现；有新内容时只出现微弱光点，不使用聊天红点、未读列表或“TA 发来一条消息”的叙事。

### 容器动作

- 星星瓶：持续按住瓶子抽取一颗，松手过早不会打开。
- 胶囊盒：持续按住后打开一颗，不使用传统主按钮。
- 纸团篓：持续按住更长时间才展开，任何时候松手都会中止；不再弹出系统确认框。
- 留下自己的内容：拿起一个空白物件进入制作场景，而不是进入表单页。

### 文字

文字写在一张会根据容器变化的纸面上。完成后把星星、胶囊或纸团向上推入容器口，超过触发距离后才保存；没有单独的“发送”或“确认”按钮。字符计数只在接近上限时出现。

### 照片

照片以相纸形式选择和预览，可在相纸下方留一句简短文字，然后通过同一套向上放入动作封存。1.1.0 不再创建新视频内容，以降低媒体体积和交互复杂度；1.0.0 已经保存的视频仍可读取和播放，不会丢失。

### 语音

语音是独立的专注模式：

- 第一次进入语音模式时先完成麦克风授权，避免按住后仍在等待系统权限。
- 按住开始录音。
- 向左滑动超过取消阈值后，松手取消。
- 松手结束并直接封存到当前容器，不再出现二次确认按钮。
- 红点明确表示正在录音。
- 时长以十分之一秒更新，例如 `0:02.9`。
- 波形根据 `AVAudioRecorder` 的实时电平变化。
- 录音太短、权限不可用和保存失败均有非系统弹窗式的场景内反馈。
- 保存失败时语音草稿会保留，可再次向上推入或丢弃重录。
- 从按下到松开属于专注状态，期间关闭和模式切换暂时失效。
- VoiceOver 可通过“开始录音”“完成并放入”“取消录音”三个辅助动作完成同一流程。
- 来电、音频会话中断或进入后台时会立即丢弃未完成录音，并在返回后给出场景内说明。

### 打开与回应

打开后的内容使用完整的拆星星、分开胶囊或摊平纸团动画，再展示正文与媒体。回应不再是内容下方的回复输入框；用户需要重新折一颗星星、封一颗胶囊或揉一个纸团放回共同空间，保持“共同容器”而非聊天线程的心智模型。

## 仍然保留的 1.0 能力

- 原生 SwiftUI，最低支持 iOS 17。
- 星星瓶使用 SpriteKit 与 CoreMotion 提供轻量物理反馈。
- CloudKit 自定义记录区、区域级 `CKShare` 和系统邀请。
- 创建者私有数据库与参与者共享数据库。
- 私有/共享数据库各自的 `CKSyncEngine`。
- 离线本机缓存、待上传恢复、基础冲突合并和账号变化处理。
- 文字、照片、语音写入；旧视频兼容读取。
- 容器变化和“对方已打开”弱提醒，通知不展示正文。
- 共同空间管理、参与者退出和创建者删除。
- “我的抽屉”查看自己留下的内容及打开状态。
- 无第三方依赖，无自建业务服务器。

## 数据与版本兼容

1.1.0 没有修改 CloudKit 记录类型或字段，仍使用 `DearUsRelationship`、`DearUsItem` 和系统 `cloudkit.share`。因此 1.0.0 已有关系、文字、照片、视频、语音及打开状态可以直接继续使用，不需要迁移或重新创建共同空间。

`DearUsItem.attachmentKind` 继续保留 `image`、`video` 和 `audio` 三种值；1.1.0 只是不在新建界面提供视频入口。正文、作者标识和打开状态仍写入 `CKRecord.encryptedValues`，媒体仍使用 `CKAsset`。

## 工程结构

```text
DearUs/
├── App/          App 生命周期、共享邀请、推送与本地提醒
├── Models/       关系、内容、附件与界面状态模型
├── Store/        CKSyncEngine、CKShare、离线队列与冲突处理
├── Storage/      本机状态、媒体文件与实时电平录音
├── Views/        共同房间、制作、打开、抽屉和设置
├── Components/   仪式交互、长按/拖动控件、语音手势、物件插画与物理场景
└── Design/       场景色彩、材质与光影
```

视觉与动效的长期标准见 [VISUAL_MOTION_DESIGN.md](VISUAL_MOTION_DESIGN.md)。核心物件必须先作为代码外的人工美术资产成立，SwiftUI Path、基础 Shape 和程序生成 SVG 只可用于占位或交互验证，不作为最终视觉。

核心交互组件包括：

- `RitualDepositControl`：向上推入容器的提交手势。
- `HoldToOpenControl`：带连续进度与中途松手取消的打开手势。
- `VoiceHoldRecorderView`：按住录音、左滑取消和松手完成。
- `AmbientRoomBackground`：共同房间与各容器场景的统一环境层。
- `WhisperNoticeBanner`：替代核心流程中的系统 Alert。

## 开始运行

先阅读 [CLOUDKIT_SETUP.md](CLOUDKIT_SETUP.md)，修改根目录 `Config.xcconfig`，再用 Xcode 打开 `DearUs.xcodeproj`。选择自己的开发团队，确认 iCloud、CloudKit、Push Notifications 与 Background Modes 中的 Remote notifications 已启用，然后在登录 iCloud 的真机运行。

双人分享测试应使用两台真机和两个不同的 Apple 账号。1.1.0 没有 CloudKit Schema 变更；新容器仍需先在 Development 真机成功创建一次共享，确认系统类型 `cloudkit.share` 已生成，再部署到 Production。

## 静态校验

在工程根目录运行：

```bash
python3 Scripts/validate_project.py
```

脚本检查 Swift 语法、plist、entitlements、Privacy Manifest、资源 JSON、App Icon、Xcode 工程引用、1.1.0 版本号、构建号、核心手势组件，以及核心 Views 中是否残留 `.alert`、`.confirmationDialog` 和旧分段筛选器。

Linux 环境没有 Xcode、iOS SDK、签名身份和你的 CloudKit 容器，因此静态校验不等于真机编译。发布前必须按 [RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md) 完成 Xcode Build、签名、两账号邀请、录音手势、离线同步与 Production 验收。

## 版本

当前版本为 `1.1.0`，构建号为 `2`。变更记录见 [CHANGELOG.md](CHANGELOG.md)。
