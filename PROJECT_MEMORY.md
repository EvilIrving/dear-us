# Project Memory

## 交互与 UX 文案以连续、直接、可恢复为准 · 2026-08-28 18:04 · gpt-5.6-sol

用户确立了长期交互原则：操作采用直接操控，反馈近乎即时，出错可轻松恢复；流程尽量无弹窗、无等待、无跳跃，保持连续。界面使用清晰短文案，减少干扰，相关信息成组，强调当前状态和下一步，删除产品宣言与不必要细节。动效仅用于按压、拖动阈值、打开、保存、错误等状态变化，不做循环装饰；需要让状态更清晰、生动时才使用。

实现上以本机持久化作为“放入/打开”完成点，CloudKit 后台同步，不阻塞当前操作；失败时保留草稿并支持立即重试。空状态必须说明当前为空以及内容如何出现，核心价值入口保持可见。

## 核心功能优先，容器统一使用简单占位 · 2026-08-28 17:29 · gpt-5.6-sol

用户明确决定当前阶段不要再被玻璃瓶等美术卡住，优先把整个软件逻辑做完整。星星瓶、胶囊盒、纸团篓在首页、详情、加载和引导场景可统一使用方块或简单几何模型占位，只需清楚表达类型、数量、可打开状态，并保留点击、放入、长按打开等核心交互。除非用户之后明确重启美术开发，不要主动恢复写实玻璃、复杂 3D 或物理容器。

本条取代下方「纸团篓改为真实 3D 物理」的当前方向。该 SceneKit/USDZ 实现已从工作区与 Xcode 工程移除；旧条目仅保留历史背景。

## 纸团篓改为真实 3D 物理 · 2026-08-26 10:25 · grok

用户明确说旧文档里「不要办公室垃圾桶 / 不要 3D」作废。纸团篓要真实一点：USDZ 垃圾桶、拖动微倾、纸团用物理世界掉进去并在篓里滚动。

当前实现：`PaperBinPhysicsView`（SceneKit，iOS 17），资源 `DearUs/Resources/Wireframe_Trash_Bin.usdz`。详情页可拖动倾斜；首页是缩小的同一场景、不抢点击。纸团数量跟 `paper` 容器 count 走，新增会从上方抛入。

## 本轮停在「能看见但太丑」· 2026-08-26 02:20 · grok

用户明确说杯子还是太丑，准备睡觉，要求先更新文档。外形仍未过关，不要把下面任何被否决的杯子视觉再搬回来。本条取代此前各条里「当前改成某某方案」的说法；开合物理约束仍以「玻璃杯开合：已尝试并被用户否决的方案 · 2026-08-26 01:15」为准。

### 还有效的目标

demo 只做星星瓶的外形和开合。盖沿合上时必须挡住瓶口；打开后颈口不能变短，塞子跟着盖子走；揭盖应是人手弧线，不是直线上提。动效原理可参考 `/Users/actor/Documents/code/01-learn/GestureGuide`（弹簧、1:1 跟手、松手带速度、rubber band、吸附）。`js-design-terms` 几乎没有可抄的揭盖物理。

### 本轮新否决（不要再用）

- SwiftUI Shape 插画玻璃杯：假。
- 写实照片做开合（合盖图 / 开口图 / 飞盖）：太平面，两张图叠。落地前就被否掉。
- Metal SDF 光线步进玻璃：撑满屏的乳白 CG 塑料瓶。
- iOS 26 `glassEffect` 配奶油底：白底不透明，看不清。
- 近黑房间 + 几乎全透明杯身 + 细白描边：只剩线框，看不见杯子。
- 杯身上贴一条白色高光长条：假。已从当前代码里删掉，不要加回来。
- 中明度墙 + 深色桌 + Canvas 海玻璃杯壁（外轮廓减内轮廓）：人能看见杯子了，用户仍说太丑。这是停下时屏幕上的东西，不是过关方案。

更早的否决仍然有效：Canvas 椭圆拼玻璃、分图抠图合成、合盖静帧淡出再叠飞盖、SceneKit 旋转体 PBR、视频抽帧。

### 可见性（丑可以重做，看不见不行）

不一定要黑房间。但墙、桌、杯子之间必须有对比，杯子要像桌上的物件，不要撑满屏，也不要白底乳白或黑底细线。玻璃若画厚度，应用杯壁颜色，不要整杯填成一块白，也不要只描边。

### 停下时的代码

- `DearUs/Views/StarJarExperienceView.swift`：场景、拖盖。
- `DearUs/Views/GlassJar.swift`：墙、桌、窗、Canvas 海玻璃瓶和盖。
- `DearUs/Engine/LidModel.swift` + `Spring.swift`：GestureGuide 弹簧揭盖。视觉被否，这套手感没有被单独否掉。
- 工程 `IPHONEOS_DEPLOYMENT_TARGET` 现为 26.0。
- Metal shader、贴图开合、SceneKit 都不在工程里。

下次若重做外形，从零画或换材料，不要在这只海玻璃线稿上继续贴高光。

## 杯子要让人一眼看见：墙、桌、有厚度的杯壁 · 2026-08-26 02:12 · grok

用户说不一定要黑房间，但房间和杯子必须让人能看见杯子。白底乳白、黑底细线框都等于看不见。可用做法：中明度墙 + 更深的桌面，玻璃杯壁用海玻璃色填厚度（外轮廓减内轮廓），中间仍能透出墙面，不要只描边。

## 白底不透明杯子看不清，必须深色房间 + 能看穿的玻璃 · 2026-08-26 02:05 · grok

用户明确说这几次都看不清：白底、杯子也不透明。奶油纸背景 + 乳白玻璃 / Liquid Glass / SDF 白塑料都不行。重做约束：

- 不要浅色底配浅色杯子。
- 玻璃必须能看见后面的房间（低不透明度），轮廓用亮边，不能填成一块白。
- 杯子仍是桌上小物件，不要撑满屏。

当前房间改为深色，左上暖光，杯身几乎透明，白描边。

## Metal SDF 玻璃杯已否决：撑满屏的白色塑料瓶 · 2026-08-26 01:55 · grok

光线步进 SDF 在模拟器里是一只巨大的乳白色 CG 瓶，盖子像塑料筒悬在上方，看起来廉价。用户否决。以后不要再走 SceneKit 旋转体、Metal SDF、shadertoy 玻璃。

同时不要再让杯子撑满屏幕。杯子应是桌上的小物件。当前改成 iOS 26 `glassEffect`：杯身 `.clear`、盖子 `.regular.interactive()`，盖子仍用 GestureGuide 弹簧揭开，不是贴图。

## 插画杯子和照片叠合均已否决 · 2026-08-26 01:50 · grok

用户否决了插画 Shape 杯子（「跟假的一样」），并在照片方案落地前明确否定：真实图片拿上去只能两张图叠在一起，太平面，不行。这与更早的「不要图片架构 / 不要两张图叠合」一致，以后不要再走合盖照片、开口图、飞盖精灵或抠图合成。

当前改成 **同一空间里的玻璃体**：Metal 光线步进 SDF，杯身和盖子是同一套三维玻璃（折射、厚度、菲涅尔），盖子用 GestureGuide 弹簧在这个空间里揭开，不是贴图。文件：`DearUs/Shaders/GlassJar.metal`。

## 玻璃杯重做：插画物件 + GestureGuide 物理 · 2026-08-26 01:35 · grok

用户清空旧杯子后要求重做，并指定以 `01-learn/GestureGuide`（Astro/JS 动效指南）为动效原理参考。本次没有沿用任何已被否决的路线。

### 采用的做法

- **外形**：SwiftUI Shape 画一只带盖玻璃瓶（杯身轮廓路径 + 独立盖子：钮、盖沿、塞子）。杯身和盖子共用 `GlassJarMetrics`。合上时盖沿画在瓶口之上并宽于瓶口；打开后塞子跟着盖子走，颈口高度不变。这是插画物件，不是照片、不是 SceneKit、不是序列帧。
- **动效**：移植 GestureGuide / Chenglou 模型。拖动时盖子 1:1 跟手；越过边界用 rubber band；松手采样最近 100ms 速度；用弹簧吸附到「合上」或「放到一侧」（方向随甩动）。弹簧 `F = -k(pos-dest) - bv`，固定 4ms 步长。轻点不打开，只做一下弹性提示。
- **不要再改回**：duration/easing 的 `withAnimation` 开盖、两张图叠合、抽帧、SceneKit 旋转体。

### 关键文件

- `DearUs/Engine/Spring.swift` — 弹簧、rubber、速度缓冲
- `DearUs/Engine/LidModel.swift` — 手势状态机 + CADisplayLink
- `DearUs/Views/GlassJar.swift` — 瓶与盖的外形
- `DearUs/Views/StarJarExperienceView.swift` — 场景与拖动手势

## 玻璃杯开合：已尝试并被用户否决的方案 · 2026-08-26 01:15 · grok

现阶段目标仍是 demo：先把带盖玻璃杯的外形和开合做好。用户已明确要求从零重做，并且当前代码已清空（`StarJarExperienceView` 只剩背景，无杯子）。下面这些路线都做过，用户都否决了。未来 Agent 不要再沿用，除非用户另说。

### 否决结论（必须遵守）

- 不要用「两张图叠合」做开合：合盖图 + 开口图、杯身图 + 盖子图、合盖静帧淡出再叠飞盖，都不行。
- 不要用视频抽帧 / 序列帧刷开合。用户明确说不要再走图片架构。
- 不要用廉价 3D 玻璃网格（SceneKit 旋转体 + PBR 透明）冒充实物。
- 不要用简陋 Canvas 椭圆拼一个「看起来像杯子」的矢量玻璃。
- 合上时必须像实物：盖沿挡住瓶口，不能透过盖子再看到一圈杯边。
- 打开后瓶口/颈口不能变短，盖子扣进瓶口的那截玻璃要跟着盖子走，不能消失。
- 开合不能只是直线上提；用户要的是人手揭盖的弧线（先离口，再沿肘腕弧线带到一侧，盖身绕钮倾斜）。

### 已做过、已被否决的具体尝试

1. **SwiftUI Canvas 手绘玻璃杯 + 椭圆盖**  
   杯身路径、壁厚、高光、椭圆盖，轻触/上提开合，盖子 `rotation3DEffect` 绕顶边翻起。  
   否决原因：不好看，假，不像玻璃杯。

2. **生成写实照片，杯身与盖子分图抠透明后合成**  
   用 Imagine 生成药瓶式玻璃罐、玻璃盖，品红抠图进 `Assets.xcassets`，盖子按 `jarWidth * 1.08` 叠在开口杯身上。  
   否决原因：盖子和杯口比例错配（孤立盖子是另一只更厚的砂锅盖视角）；合上后两张图叠在一起，杯边仍能看见；开合曲线也不对。

3. **以用户认可的合盖照片为比例基准，合盖静帧 + 开口罐 + 飞盖**  
   关着只显示合盖完整照片；揭开后淡出到开口罐（后来改回未裁短颈口的开口原图）并让盖子精灵沿贝塞尔弧飞出。  
   否决原因：本质仍是贴图开合，不可靠；揭开瞬间对位会跳；AI 去盖图还把「扣的口子」画短了。

4. **SceneKit 三维旋转体玻璃杯**  
   删贴图，用剖面 lathe 做有厚度的罐和带塞子的盖，同一套几何，盖沿在深度上挡住瓶口，塞子是盖子网格的一部分，开合走 3D 贝塞尔。  
   否决原因：更不行。看起来像廉价 CG，不是用户要的质感。

5. **合盖照片 `image_to_video` 再抽帧 scrub**  
   锁定机位只动盖子，约 37 帧放进 `LidFrames`，手势/轻触在 0…1 上刷帧。画面连续性其实接近人手揭盖（先离口再弧到一侧，塞子可见，颈口完整）。  
   否决原因：用户不要这种图片架构；要求删了从 0～1 重做。

6. **再次纯 Canvas 几何（无图片）**  
   罐和盖共用 `JarLayout`，盖沿宽于瓶口并画在上层，塞子画在盖上，开合仍是 2D 弧 + 绕钮旋转。  
   否决原因：还是不行。随即按用户要求全部删掉，只留空白底，等下一条任务再做。

### 动效参考项目的可用点（尚未落地）

用户曾指向 `/Users/actor/Documents/code/js-design-terms`。该仓库本身几乎没有复杂动效，不是「揭盖」范本。能借鉴的只有很轻的交互气质，且当时并未用上：

- 页面进入：`0.2s ease`，`opacity` + `translateY(6px)`，短、不弹。
- 按钮：`0.16s ease`，按下 `translateY(1px)`，像实物被按下去，不是弹簧玩具。
- Toast：`0.2s` 从下方 14px 到位。
- `prefers-reduced-motion` 时动画基本关掉。
- 视觉是暖纸色、浅阴影、卡片物体感，不是 3D 渲染玻璃。

里面**没有**惯性滚动、弹簧物理、手绘路径或 3D 盖子可抄。不要把那个项目理解成已经有一套可移植的开盖引擎。

### 当前仓库状态

- `DearUs/Views/StarJarExperienceView.swift`：仅 `PrototypeTheme.roomGradient`，无杯子。
- 贴图 imageset、`LidFrames`、SceneKit 实现均已删除。
- 用户指示：先空着，下一条任务再重新做。重做时不要复活上述任一被否决架构。
