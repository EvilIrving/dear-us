# 1.1.0 发布检查清单

## 账号与签名

- [ ] `Config.xcconfig` 已替换为正式 Bundle ID、CloudKit Container ID 和 Team ID。
- [ ] App ID 已关联正确的 iCloud Container。
- [ ] iCloud/CloudKit、Push Notifications、Background Modes · Remote notifications 已在 Xcode 启用。
- [ ] Debug 真机签名成功，无 entitlement 或 provisioning profile 警告。
- [ ] Archive 使用 Distribution 签名，最终 entitlement 含正确 iCloud Container 与 production APNs 环境。
- [ ] `MARKETING_VERSION` 为 `1.1.0`，`CURRENT_PROJECT_VERSION` 为 `2`。

## 编译与基础布局

- [ ] Xcode Clean Build Folder 后，Debug 与 Release 均可编译。
- [ ] iPhone SE 尺寸、主流 6.1/6.7 英寸 iPhone 与 iPad 无截断或不可达控件。
- [ ] 键盘出现时制作场景可滚动，文字纸面、模式切换和向上放入手势仍可到达并操作。
- [ ] 前后台切换、来电/音频中断和锁屏后，录音状态不会卡住。
- [ ] Reduce Motion、动态字体、深浅外观和 VoiceOver 至少完成一次真机检查。

## 共同房间

- [ ] 首页不出现消息列表、功能卡片、未读红点或聊天式通知文案。
- [ ] 星星、胶囊、纸团数量变化能通过容器内容积累体现。
- [ ] 新内容只显示微弱光点，且 VoiceOver 能说明容器当前状态。
- [ ] 下拉刷新可以触发同步，但不会破坏容器动画状态。
- [ ] 我的抽屉、设置和同步入口均可到达且可返回共同房间。

## 文字写入

- [ ] 三个容器都能进入对应的全屏纸面书写场景。
- [ ] 空内容不能触发向上放入。
- [ ] 向上拖动未越过阈值时松手不会保存。
- [ ] 越过阈值有触觉反馈，松手后只保存一次。
- [ ] 键盘未收起时开始拖动，输入焦点会正确退出。
- [ ] 接近 4,000 字上限时才显示剩余字数，超过上限会截断。
- [ ] 保存失败后内容仍保留在当前场景，可重试。

## 照片写入

- [ ] PhotosPicker 只展示图片，不提供新视频选择。
- [ ] 选择、取消选择、移除和重新选择照片均正常。
- [ ] 照片可带一句说明，也可只保存照片。
- [ ] 超过 48 MB 的图片会被阻止，临时文件会清理。
- [ ] 保存失败后照片草稿仍保留。
- [ ] 1.0.0 已有视频在 1.1.0 中仍能读取和播放。

## 语音手势

- [ ] 第一次进入语音模式时即出现麦克风授权；授权完成前不能误启动录音。
- [ ] 拒绝授权后显示场景内提示，不出现失控录音状态。
- [ ] 按住开始录音；短暂触碰不会意外保存。
- [ ] 红点只在真实录音期间点亮。
- [ ] 时长按十分之一秒更新，格式如 `0:02.9`。
- [ ] 波形会随真实说话音量变化，静音时不会剧烈跳动。
- [ ] 向左滑动未达到阈值时可滑回继续录音。
- [ ] 越过取消阈值时有警告触觉；松手后临时文件被删除。
- [ ] 正常松手后立即封存，不再显示二次确认按钮。
- [ ] 小于最短时长的录音被丢弃，并给出可理解提示。
- [ ] 保存失败后语音草稿仍在，可向上推重试或丢弃重录。
- [ ] 权限准备、录音器准备和真实录音期间，关闭控件、模式切换与下拉关闭均不可打断会话。
- [ ] VoiceOver 的“开始录音”“完成并放入”“取消录音”辅助动作均可独立完成。
- [ ] 蓝牙耳机、听筒/扬声器切换以及其他 App 音频恢复正常。

## 打开与回应

- [ ] 星星和胶囊必须持续按住到进度完成；提前松手不会打开。
- [ ] 纸团需要更长的持续按住，任何时候提前松手都不会展开。
- [ ] 纸团流程中没有系统 `confirmationDialog`。
- [ ] 打开动作只消耗一次额度，快速重复手势不会重复打开。
- [ ] 星星随机抽取、胶囊和纸团较早内容优先符合 Store 逻辑。
- [ ] 拆星星、分胶囊、摊纸团动画完成后才显示内容。
- [ ] 向下拉或关闭控件都能收好内容。
- [ ] “回应”会进入新的同类制作场景，而不是在原内容下方形成聊天线程。
- [ ] 自定义语音波形可播放、暂停和拖动定位。

## CloudKit 与双机

- [ ] Development 中已生成 `DearUsRelationship`、`DearUsItem` 和系统类型 `cloudkit.share`。
- [ ] 1.1.0 未新增字段；若是全新容器，Schema Changes 已部署到 Production。
- [ ] 两台真机、两个不同 Apple 账号完成创建、邀请和接受流程。
- [ ] 文字、照片、语音均可从 A 同步到 B，再从 B 同步打开状态回 A。
- [ ] 1.0.0 数据升级到 1.1.0 后，关系、额度、打开状态和媒体均保留。
- [ ] 两端版本一新一旧时，至少验证文字、照片、语音和打开状态的兼容边界。
- [ ] 停止共享、参与者退出、创建者删除共同空间均已测试。
- [ ] 重新安装或同账号新设备登录后能找回现有共同空间。

## 网络与恢复

- [ ] 离线文字、照片和语音写入后，恢复网络可以上传。
- [ ] 离线打开后恢复网络可以同步打开状态。
- [ ] App 被杀死后收到 CloudKit 变化，重新进入仍能拉取数据。
- [ ] iCloud 暂时不可用、账号退出和账号切换不会把旧账号内容上传到新账号。
- [ ] iCloud 配额不足、附件丢失和权限不足均显示场景内错误提示。
- [ ] 临时录音与照片文件在成功、取消和失败路径下均无明显泄漏。

## 提醒与隐私

- [ ] 通知正文不包含用户文字、文件名、照片、语音或纸团情绪类别。
- [ ] 通知关闭、首次授权和从系统设置恢复授权均正常。
- [ ] App Store Connect 隐私标签与 CloudKit、通知、麦克风和用户内容处理一致。
- [ ] 已提供隐私政策与支持页面。

## App Store 与 TestFlight

- [ ] App 名称、图标、版本 `1.1.0`、构建号 `2` 与 Bundle ID 已确认。
- [ ] 审核备注说明：产品需要两个不同 iCloud 账号测试共享，没有自建账号系统。
- [ ] 审核备注说明：视频仅为旧内容兼容读取，1.1 新建入口只包含文字、照片和语音。
- [ ] TestFlight Production 环境完成一次全新安装双机验收。
- [ ] 从 1.0.0 TestFlight 原地升级到 1.1.0 完成一次数据兼容验收。
